apiVersion: batch/v1
kind: Job
metadata:
  name: someJob
spec:
  template:
    spec:
      containers:
      - name: someJob
        image: busybox
        command: ["sleep","60"]
      restartPolicy: Never
  backoffLimit: 0